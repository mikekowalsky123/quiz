{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(quizForm) }}
        {% for i in 0..(quizForm.questions|length - 1) %} 
            {{ form_row(quizForm.questions[i], { 'label': questions[i].question}) }}
        {% endfor %}
        {{ form_row(quizForm.submit) }}
    {{ form_end(quizForm) }}
{% endblock %}